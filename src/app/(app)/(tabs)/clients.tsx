// Unreachable — the "Clients" tab is inert (see (tabs)/_layout.tsx). No
// client-browsing feature exists yet; this file only needs to exist so the
// tab bar has a route to point at.
export default function ClientsScreen() {
  return null;
}
