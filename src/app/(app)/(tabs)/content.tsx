// Unreachable — the "Content" tab is inert (see (tabs)/_layout.tsx). No
// promo-content upload feature exists yet; this file only needs to exist so
// the tab bar has a route to point at.
export default function ContentScreen() {
  return null;
}
