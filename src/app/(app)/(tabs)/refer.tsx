// Unreachable — the "Refer" tab is inert (see (tabs)/_layout.tsx). Referral
// code sharing lives on the Home screen; this file only needs to exist so
// the tab bar has a route to point at.
export default function ReferScreen() {
  return null;
}
